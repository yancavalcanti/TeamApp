<?xml version="1.0" encoding="utf-8"?>
    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        tools:context="com.example.yan.teamapp.MainActivity"
        tools:showIn="@layout/activity_main">

        <ListView
            android:id="@+id/playerList_listPlayers"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        </ListView>

    <Button
        android:id="@+id/playerInsert_callButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:text=""
        android:textColor="#ffffff"
        android:textSize="40sp"
        android:elevation="6dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="86dp"
        android:layout_marginRight="16dp"
        android:background="@drawable/ic_phone" />

    <Button
        android:id="@+id/playerInsert_buttonNew"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:text="+"
        android:textColor="#ffffff"
        android:textSize="40sp"
        android:elevation="6dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginBottom="16dp"
        android:layout_marginRight="16dp"
        android:background="@drawable/this_background_button" />

</RelativeLayout >
